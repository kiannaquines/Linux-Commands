## Docker compose stop and remove containers
docker-compose down --volumes

## Docker compose one liner command
docker-compose build && docker-compose up -d

## Show images
docker image ls

## Run container image
docker run -it <REPO_NAME>

## Delete Images
docker rmi <id1> <id2>

